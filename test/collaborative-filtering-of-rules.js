/* global describe it */
const expect = require('chai').expect;

describe('collaborative filtering', () => {
  it('ranks rules from an installation with similar other rules higher', () => {
    expect(false).to.eq(true);
  });

  it('ranks rules from an installation with similar other devices higher', () => {
    expect(false).to.eq(true);
  });
});
