/* global describe it beforeEach */
var assert = require('assert'),
    RuleGenerator = require('../src/RuleGenerator');

describe('RuleGenerator', () => {
  describe('#generate()', () => {
    beforeEach(() => {

    });

    it('should be higher for closer values', () => {
      assert(true);
    });
  });
});
